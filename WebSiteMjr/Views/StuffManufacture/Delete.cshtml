@model WebSiteMjr.Domain.Model.StuffManufacture

@{
    ViewBag.Title = "Deletar fabricante de Materiais";
    Layout = "~/Views/Shared/_ClientLayout.cshtml";
}

<h2>@ViewBag.Title</h2>

<h3>Tem certeza que deseja deletar o fabricante @Model.Name?</h3>
<fieldset>
    <legend>Fabricante</legend>

    <div class="display-label">
        @Html.LabelFor(model => model.Name, "Nome")
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Name)
    </div>
</fieldset>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Voltar a listagem", "Index")
    </p>
}